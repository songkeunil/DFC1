<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.djplat.project.common.dao.SecurityDAO">


	<select id="selectUser" resultType="userVO">
		SELECT * FROM MEMBER WHERE MEMBER_ID=#{param1}
	</select>
	
	<select id="getAuthList" resultType="String">
		SELECT MEMBER_RIGHT FROM MEMBER WHERE MEMBER_ID=#{param1}
	</select>


	<insert id="userSignUp">
		INSERT INTO MEMBER(MEMBER_ID, MEMBER_PW, MEMBER_ENABLED, MEMBER_NAME,MEMBER_GENDER,MEMBER_BIRTH,
		MEMBER_PHONENO,MEMBER_EMAIL,MEMBER_ADDRESS) 
		VALUES(#{param1},#{param2},#{param3},'홍길동','M','22/03/02',010,'1111','1111')
	</insert>
	
	<insert id="giveAuth">
		INSERT INTO MEMBER(MEMBER_ID, MEMBER_RIGHT) VALUES(#{param1},#{param2})
	</insert>
  
</mapper>