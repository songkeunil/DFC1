<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.admin">

           
<select id="memberList" resultType="memberVO" >
    <![CDATA[
    select * from
     (select rownum as recNum, member_ID,MEmber_phoneno,member_last_log,member_name,member_email from member order by member_id desc)
     where recNum between(#{section}-1)*100  + (#{pageNum}-1)*10+1 and (#{section}-1)*100+#{pageNum}*10
      ]]>
</select>
<update id="deleteMember" parameterType="String">
  		UPDATE MEMBER
  		SET MEMBER_ENABLED='0'
  		WHERE MEMBER_ID = #{member_id}
  	</update>


<select id="totalMembers" resultType="int">
	<![CDATA[
	select count(member_id) from MEMBER
	]]>
</select>


<update id="modifyMemberInfo" parameterType="java.util.HashMap">
	   update member
			   <set>
			      <if test=" member_pw!='' and member_pw!=null">
			        member_pw=#{member_pw},
			      </if>
			      
			       <if test=" member_birth!='' and member_birth!=null">
			         member_birth=#{member_birth},
			      </if>
					<if test=" member_phoneno!='' and member_phoneno!=null">
			        member_phoneno=#{member_phonenos},
			      </if>	     
			   </set>
		where 
		member_id=#{member_id}
	</update>	
</mapper>