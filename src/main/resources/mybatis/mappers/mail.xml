<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.mail">

<!-- 	<insert id="createAuthKey">
		insert into MEMBER_AUTH values(#{memberEmail},#{authKey})
	</insert> -->
<update id="memberAuth">
<![CDATA[
	update MEMBER set MEMBER_AUTH = 1 where (select count(*) from member_right where MEMBER_EMAIL=#{memberEmail})>0]]>
</update>
  
</mapper>